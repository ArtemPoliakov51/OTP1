package view;

/**
 * Interface to help group together all the views so they can efficiently use the {@link UIComponent} class.
 * Classes implementing this interface must implement the openView method.
 */
public interface UIView {
    void openView();
}
